Play repository containing two things.

* A new unittest library/framework based on seatest. This will change to have better output formats and be a pure C99 unittest library.
* Eventually the real project will be somewhat of a VI clone but we will see if it ever comes that far.
